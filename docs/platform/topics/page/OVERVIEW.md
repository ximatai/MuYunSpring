# 页面专题

页面专题覆盖动态低代码页面的交付和交互编排。它的目标不是新增一套页面内核，而是让当前运行态模块配置和已发布 UI/query 配置通过菜单入口、页面 bootstrap、动态 Web 和动作上下文形成可运行的前端契约。静态模块 UI 声明与读投影的统一路线见 [UI 声明与读投影设计](../../../architecture/UI_DECLARATION_AND_READ_PROJECTION.md)。

本专题只描述页面业务线索和 Web 交付面，不展开 Java 底层设计。动态页面依赖的数据读写、权限、数据范围、审计、租户、事务、附件关系和动作执行，仍回到平台统一能力链路。

## 专题分层

| 目录           | 关注点                                                                                                |
| -------------- | ----------------------------------------------------------------------------------------------------- |
| `delivery/`    | 页面交付闭环：菜单入口、bootstrap、列表查询、汇总、表单保存、附件关系、查重、引用候选、页面偏好       |
| `interaction/` | 页面交互编排：关联视图、局部编辑、动态弹窗、action block、模块任务、任务检查、evaluate 和公式即时试算 |

总入口只保留横向索引。具体能力说明分别见：

- [页面交付概览](delivery/OVERVIEW.md)
- [页面交付 Web API](delivery/WEB_API.md)
- [页面交互概览](interaction/OVERVIEW.md)
- [页面交互 Web API](interaction/WEB_API.md)

## 核心链路

```text
菜单节点
  -> /platform.menu/{menuId}/entry
  -> 页面 bootstrap
  -> 已发布 UI 配置 / 查询模板 / 动作区块
  -> /{moduleAlias} 动态 Web
  -> DynamicRecordService / 动态动作上下文
```

页面入口只定位模块、页面模式、客户端类型、默认 UI 配置、默认查询模板和入口参数。真实数据访问、动作授权、记录数据范围、字段保护、乐观锁和运行事件由动态运行态继续负责。

## 稳定边界

1. 在线页面只消费已发布且启用的 UI 配置和查询模板；未发布配置属于配置工作区。
2. UI 配置是页面交付真相源；基础 view descriptor 保持结构兼容，不反写页面配置。
3. 页面保存直接提交动态业务记录；`uiConfigId` 仅作为页面运行上下文，不改变动态记录模型身份。
4. `record.attachments` 和附件维护接口只管理业务记录到 `fileId` 的关系；文件事实、二进制、预览和下载策略归属文件服务，移除附件关系不推导物理文件删除。
5. 标准创建、更新直接提交记录；字段引用的转正和保存后旧文件删除由平台按实体差集处理，边界见 [MuYunFileServer 与文件引用生命周期](../../FILE_REFERENCE_LIFECYCLE.md)。
6. 交互区块只识别已收口类型，不把 `layoutJson.blocks` 扩展成任意前端组件注册表。
7. 所有业务动作必须回到动态 action 主链路，不能用前端私有 handler 或模块专用 controller 绕过权限、数据范围和审计。
8. 页面偏好只影响当前用户体验，可承载列表列宽、排序、筛选和个人化导出配置，不改变平台 UI 配置真相源。

## 并发编辑提示

静态和动态记录编辑页都应把详情读取到的 `version` 保留到保存 payload。前端草稿归一化必须以当前 draft/record 为基底，只覆盖需要修剪或默认化的业务字段，不能重建一个丢失 `id`、`version` 等标准字段的对象。

编辑页如已进入记录详情或抽屉上下文，应优先在当前编辑区域处理 `CONFLICT_VERSION`：标记当前记录已被外部变更，并提示用户重新加载。只有页面无法确认当前编辑记录、当前不是编辑态、或局部 handler 明确返回未接管时，才回退到全局错误展示。

实时数据变化只负责把“记录可能已变更”的事实送达页面，不携带 UI 指令。页面根据当前编辑状态、保存状态和记录 ID 判断是否展示外部变更提示。保存失败后的版本冲突和实时收到的记录变化应复用同一类页面状态，避免登录态、数据变更和业务动作各自实现一套提示机制。

## 关联专题

| 专题         | 关系                                                                         |
| ------------ | ---------------------------------------------------------------------------- |
| 平台配置     | 提供模块、元数据、菜单、UI 配置、查询模板和发布快照。                        |
| 动态运行态   | 提供 `/{moduleAlias}` 下的描述、查询、保存、引用、动作和 OpenAPI。           |
| 身份与权限   | 提供菜单剪枝、动作授权、记录数据范围和当前用户上下文。                       |
| 附件与文件   | 页面只维护业务附件关系；文件服务负责文件事实和访问能力。                     |
| 工作流与任务 | 模块任务不是审批待办；后续审批状态和工作流上下文可作为平台事实接入任务判定。 |
