package net.ximatai.muyun.spring.ability.logging;

/** The four business facts collected by the first logging phase. */
public enum BusinessLogEventType {
    LOGIN,
    ACTION,
    REQUEST_ERROR,
    PAGE_ACCESS
}
