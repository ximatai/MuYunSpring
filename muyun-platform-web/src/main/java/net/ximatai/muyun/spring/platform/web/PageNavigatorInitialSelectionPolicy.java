package net.ximatai.muyun.spring.platform.web;

/** Explicit initial selection behavior after a navigator source's ordered records load. */
public enum PageNavigatorInitialSelectionPolicy {
    NONE,
    FIRST_RECORD
}
