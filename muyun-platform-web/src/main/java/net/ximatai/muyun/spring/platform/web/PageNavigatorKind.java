package net.ximatai.muyun.spring.platform.web;

/** Supported navigator presentations. Both publish the same selected-scope query contract. */
public enum PageNavigatorKind {
    TREE,
    MICRO_LIST
}
