package net.ximatai.muyun.spring.platform.web;

/** Presentation behavior when a navigator source has exactly one accessible record. */
public enum PageNavigatorSingleResultPolicy {
    NONE,
    AUTO_SELECT,
    AUTO_SELECT_AND_HIDE
}
